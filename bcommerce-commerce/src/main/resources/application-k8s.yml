# Kubernetes 集群内连接（SPRING_PROFILES_ACTIVE=k8s）
spring:
  elasticsearch:
    uris: http://elasticsearch:9200
  datasource:
    url: jdbc:mysql://mysql:3306/bcommerce?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: redis
      port: 6379
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
