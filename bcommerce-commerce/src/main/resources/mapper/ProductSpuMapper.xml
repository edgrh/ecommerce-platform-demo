<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcommerce.mapper.ProductSpuMapper">
    <select id="searchByKeyword" resultType="com.bcommerce.model.ProductSpu">
        SELECT id, category_id AS categoryId, title, subtitle, detail, merchant_id AS merchantId, status, created_at AS createdAt
        FROM bc_product_spu
        WHERE status = 'ON_SHELF'
          AND title LIKE CONCAT('%', #{q}, '%')
        LIMIT #{limit}
    </select>
</mapper>
