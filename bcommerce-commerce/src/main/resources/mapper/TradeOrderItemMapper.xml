<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcommerce.mapper.TradeOrderItemMapper">

    <insert id="insert">
        INSERT INTO bc_trade_order_item_${shard}
        (id, order_id, user_id, sku_id, quantity, unit_price_cent, title_snapshot)
        VALUES
        (#{row.id}, #{row.orderId}, #{row.userId}, #{row.skuId}, #{row.quantity}, #{row.unitPriceCent}, #{row.titleSnapshot})
    </insert>

    <select id="listByUserAndOrder" resultType="com.bcommerce.model.TradeOrderItem">
        SELECT id, order_id AS orderId, user_id AS userId, sku_id AS skuId, quantity,
               unit_price_cent AS unitPriceCent, title_snapshot AS titleSnapshot
        FROM bc_trade_order_item_${shard}
        WHERE order_id = #{orderId} AND user_id = #{userId}
    </select>
</mapper>
