apiVersion: apps/v1
kind: Deployment
metadata:
  name: elasticsearch
  namespace: bcommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: elasticsearch
  template:
    metadata:
      labels:
        app: elasticsearch
    spec:
      containers:
        - name: elasticsearch
          image: bcommerce/elasticsearch:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9200
          env:
            - name: discovery.type
              value: single-node
            - name: xpack.security.enabled
              value: "false"
            - name: ES_JAVA_OPTS
              value: "-Xms512m -Xmx512m"
          resources:
            requests:
              memory: "1Gi"
              cpu: "250m"
            limits:
              memory: "1536Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: bcommerce
spec:
  selector:
    app: elasticsearch
  ports:
    - port: 9200
      targetPort: 9200
